# third-party-extensions/pg_duckdb/test/regression/yb_schedule
#
# YB-authored tests for lake_io execution mode: lake import, the restrictions,
# and regression guards for fixed backend crashes.
test: yb.orig.lake_import
test: yb.orig.lake_io_restrictions
test: yb.orig.pg_duckdb_crash_guards
test: yb.orig.pgaudit_duckdb

# Guard for #32655 (backend FATAL when DuckDB scans a YB table on a worker thread via an ORDER BY
# export). Runs at duckdb.threads=1 (see the test) so it stays green until #32655 is fixed; drop the
# SET there once the fix lands to widen the guard.
test: yb.orig.issue_32655
