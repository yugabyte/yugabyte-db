# This template can be copied to ~/.yb_buildrc or $YB_SRC_ROOT/.git/yb_buildrc, where $YB_SRC_ROOT
# is a yugabyte-db repository directory. It contains various parameters that control the YugabyteDB
# core database build. The default values of parameters specified here are overriden by the
# corresponding command line options.

# For examples of variables you can specify here, look at the set_default_yb_build_args
# function in yb_build_cmd_line.sh.

# If you are more comfortable with command-line options as opposed to special variables, you can use
# the following lists of arguments.

# Arguments to be implicitly added to the beginning of the argument list.
# yb_build_prepend_args()

# Arguments to be implicitly added to the end of the argument list.
# yb_build_append_args=()
