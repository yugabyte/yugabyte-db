title: ClusterCustomInstanceSpec
description: |
  ClusterCustomInstanceSpec

  Custom instance spec is used to specify custom cpu and memory requests/limits for tserver and master pods of a cluster. Specified as part of ClusterSpec.
type: object
properties:
  tserver:
    $ref: "./K8SNodeResourceSpec.yaml"
  master:
    $ref: "./K8SNodeResourceSpec.yaml"
