title: ClusterProviderEditSpec
description: Edit Cloud Provider settings for the cluster. Part of ClusterAddSpec and ClusterEditSpec.
type: object
required:
  - region_list
properties:
  region_list:
    description: Edit the list of regions in the cloud provider to place data replicas
    type: array
    items:
      type: string
      format: uuid
