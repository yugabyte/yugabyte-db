title: YSQL Spec for the Universe
type: object
properties:
  enable:
    description: Whether to enable YSQL API on this Universe
    type: boolean
    example: true
  enable_auth:
    description: Whether to enable authentication to access YSQL on this Universe
    type: boolean
    example: false
