title: TaskExecutorShutdownResp
description: |
  Result of a task executor shutdown request.
type: object
properties:
  success:
    description: |
      True if task executor shutdown was initiated by this call.
    type: boolean
    example: true
x-yba-api-visibility: internal
