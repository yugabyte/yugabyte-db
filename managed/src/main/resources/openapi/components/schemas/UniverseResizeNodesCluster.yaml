title: UniverseResizeNodesCluster
description: |
  UniverseResizeNodesCluster

  A single cluster for UniverseResizeNodes.
type: object
required:
  - uuid
  - node_spec
properties:
  node_spec:
    $ref: "./ClusterResizeNodeSpec.yaml"
  gflags:
    $ref: "./ClusterGFlags.yaml"
  uuid:
    description: Cluster UUID
    type: string
    format: uuid
