title: UniversePagedQuerySpec
description: |
  Paged list of universes for a customer.
  Universes are ordered by name (case-insensitive), then universe UUID. The order is ascending by default.
type: object
allOf:
  - $ref: "./PaginationSpec.yaml"
properties:
  filter:
    $ref: "./UniverseListApiFilter.yaml"
