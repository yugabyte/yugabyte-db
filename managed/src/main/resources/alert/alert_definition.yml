groups:
  - name: yugaware
    rules:
      - alert: {{ definition_name }}
        expr: {{ definition_expr }}
        for: {{ duration }}
        labels:
{{ labels }}
        annotations:
          summary: "{{ summary_template }}"
