package common

const DefaultServiceUser string = "yugabyte"

const ServerKeyStorePath string = "server.ks"

const ServerCertPath string = "server-cert.pem"

const ServerKeyPath string = "server-key.pem"
