# Check if the yugabyte user exists
if id {{ yb_user }} &>/dev/null; then
    echo "User {{ yb_user }} exists"
    add_result "Yugabyte User Existence Check" "PASS" "User {{ yb_user }} exists"
    # FATAL: Check if existing user's home directory matches the configured  yb_user_home
    existing_home=$(getent passwd {{ yb_user }} | cut -d: -f6)
    configured_user_home="{{ yb_user_home }}"
    # Resolve both paths to absolute canonical paths for comparison
    existing_home_abs=$(realpath -m "$existing_home" 2>/dev/null || echo "$existing_home")
    configured_user_home_abs=$(realpath -m "$configured_user_home" 2>/dev/null || echo "$configured_user_home")

    if [ "$existing_home_abs" != "$configured_user_home_abs" ]; then
        fatal_msg="User {{ yb_user }} home directory"
        fatal_msg+=" '$existing_home_abs' does not match"
        fatal_msg+=" configured '$configured_user_home_abs'."
        fatal_msg+=" This mismatch can cause system corruption."
        add_fatal_result \
            "Yugabyte User Home Directory Mismatch" \
            "$fatal_msg"
    fi
else
    echo "User {{ yb_user }} does not exist"
    add_result "Yugabyte User Existence Check" "FAIL" "User {{ yb_user }} does not exist"
    # Validate that a home directory is configured for user creation
    if [ -z "{{ yb_user_home }}" ]; then
        add_fatal_result \
            "Yugabyte Home Directory Config" \
            "No home directory configured for user {{ yb_user }}"
    fi
fi

# Get the actual home directory of the yugabyte user
# Output looks like yugabyte:x:1001:1001::/home/yugabyte:/bin/bash
set_yb_user_home=$(getent passwd {{ yb_user }} | cut -d: -f6 2>&1)
if [ -z "$set_yb_user_home" ]; then
    echo "Could not find home directory for user {{ yb_user }}"
    add_result "Yugabyte Home Directory Assigned Check" "FAIL" "Could not find home directory for user {{ yb_user }}"
else
    normalized_set_yb_user_home=$(readlink -m "$set_yb_user_home" 2>&1)
    normalized_yb_user_home=$(readlink -m "{{ yb_user_home }}" 2>&1)
    if [ "$normalized_set_yb_user_home" == "$normalized_yb_user_home" ]; then
        echo "Yugabyte home directory for user {{ yb_user }} is correctly set to {{ yb_user_home }}"
        add_result "Yugabyte Home Directory Assigned Check" "PASS" "Yugabyte home directory for user {{ yb_user }} is correctly set to {{ yb_user_home }}"
    else
        echo "Yugabyte home directory for user {{ yb_user }} is incorrectly set to $set_yb_user_home"
        add_result "Yugabyte Home Directory Assigned Check" "FAIL" "Yugabyte home directory for user {{ yb_user }} is set to $set_yb_home_dir instead of {{ yb_user_home }}"
    fi
fi

check_directory_permissions() {
  local dir="$1"
  local min_space_gb="$2"
  local dir_name_msg="$3"
  if [ ! -d "$dir" ]; then
    echo "$dir does not exist"
    add_result "$dir_name_msg Existence Check" "FAIL" "$dir does not exist"
    return
  fi
  owner=$(stat -c "%U" "$dir")
  group=$(stat -c "%G" "$dir")
  permissions=$(stat -c "%a" "$dir")
  if [ "$owner" != "{{ yb_user }}" ]; then
    echo "Ownership of $dir is incorrect. Expected owner: {{ yb_user }}, Found: $owner"
    add_result "$dir_name_msg Ownership Check" "FAIL" "Ownership of $dir is incorrect. Expected owner: {{ yb_user }}, Found: $owner"
  fi
  if [ "$group" != "{{ yb_user }}" ]; then
    echo "Group ownership of $dir is incorrect. Expected group: {{ yb_user }}, Found: $group"
    add_result "$dir_name_msg Group Ownership Check" "FAIL" "Group ownership of $dir is incorrect. Expected group: {{ yb_user }}, Found: $group"
  fi
  if [ "$permissions" -lt 711 ]; then
    echo "Permissions of $dir are too restrictive. Expected: 711 or less restrictive, Found: $permissions"
    add_result "$dir_name_msg Permissions Check" "FAIL" "Permissions of $dir are too restrictive. Expected: 711 or less restrictive, Found: $permissions"
  fi
  echo "$dir has the correct ownership and acceptable permissions"
  add_result "$dir_name_msg Permissions Check" "PASS" "$dir has the correct ownership and acceptable permissions"
  free_space_gb=$(df -B1G --output=avail "$dir" | tail -n 1)
  if [ "$free_space_gb" -gt "$min_space_gb" ]; then
    add_result "$dir_name_msg Free Space Check" "PASS" "Sufficient disk space available: ${free_space_gb}G"
  else
    add_result "$dir_name_msg Free Space Check" "FAIL" "Insufficient disk space: ${free_space_gb}G available, ${threshold}G required."
  fi
}

is_subdirectory() {
  local dir="$1"
  local parent="$2"

  # Resolve the absolute canonical paths
  dir=$(realpath -m "$dir")
  parent=$(realpath -m "$parent")

  # Append a trailing slash to the parent directory for accurate matching
  case "$dir/" in
    "$parent/"* ) return 0 ;;  # dir is inside parent
    * ) return 1 ;;            # dir is not inside parent
  esac
}

# Check the correct permissions for yb_home_dir and yb_user_home
if [ "$(realpath "{{ yb_user_home }}")" = "$(realpath "{{ yb_home_dir }}")" ]; then
  check_directory_permissions "{{ yb_user_home }}" "{{ min_home_dir_space_gb }}" "Yugabyte User Home Directory"
else
  check_directory_permissions "{{ yb_home_dir }}" "{{ min_home_dir_space_gb }}" "Yugabyte Home Directory"
  check_directory_permissions "{{ yb_user_home }}" "{{ min_user_home_space_gb }}" "Yugabyte User Home Directory"
fi

threshold={{ min_mount_point_dir_space_gb }}
# Convert the space-separated string to an array
mount_points="{{ mount_points | default('') | trim }}"
IFS=' ' read -ra mount_points_array <<< "$mount_points"
# Verify each mount point
for mount_point in "${mount_points_array[@]}"; do
    if is_subdirectory "$mount_point" "{{ yb_home_dir }}"; then
        message="Mountpoint $mount_point is inside software home directory {{ yb_home_dir }}"
        add_result "home directory check" "FAIL" "$message"
    fi
    if [ -d "$mount_point" ]; then
        # Check if the directory is owned by yugabyte or has 7 (rwx) permissions
        owner_check=$(stat -c '%U' "$mount_point")
        perm_check=$(stat -c '%A' "$mount_point" | cut -c 2-4)

        if [ "$owner_check" == "{{ yb_user }}" ] || [[ "$perm_check" == "rwx" ]]; then
            result="PASS"
            message="Directory $mount_point is owned by {{ yb_user }} or has '7' (rwx) permissions."
            echo "[PASS] $message"
        else
            result="FAIL"
            message="Directory $mount_point is not owned by {{ yb_user }} and lacks '7' (rwx) permissions."
            echo "[FAIL] $message"
            any_fail=1
        fi
    else
        result="FAIL"
        message="Directory $mount_point does not exist."
        echo "[FAIL] $message"
        any_fail=1
    fi
    add_result "$mount_point Ownership/Permission Check" "$result" "$message"

    if [ -d "$mount_point" ]; then
        # Get available disk space in gigabytes
        free_space_gb=$(df -B1G --output=avail "$mount_point" | tail -n 1 )
        if [ "$free_space_gb" -gt "$threshold" ]; then
            result="PASS"
            message="Sufficient disk space available: ${free_space_gb}G"
            echo "[PASS] $message"
        else
            result="FAIL"
            message="Insufficient disk space: ${free_space_gb}G available, ${threshold}G required."
            echo "[FAIL] $message"
            any_fail=1
        fi
        add_result "$mount_point Free Space Check" "$result" "$message"
    fi
done

threshold={{ min_tmp_dir_space_gb }}
# Get available disk space in gigabytes
free_space_gb=$(df -B1G --output=avail "{{ tmp_directory }}" | tail -n 1 )
if [ "$free_space_gb" -gt "$threshold" ]; then
    add_result "Yugabyte Temp Directory Free Space Check" "PASS" "Sufficient disk space available: ${free_space_gb}G"
else
    add_result "Yugabyte Temp Directory Free Space Check" "FAIL" "Insufficient disk space: ${free_space_gb}G available, ${threshold}G required."
fi

if [ "{{ check_clean_dirs }}" == "True" ]; then
  # Check whether yugabyte home directory is cleaned up.
  if [[ -e "{{ yb_home_dir }}" ]]; then
    files=""
    if [[ "$check_type" == "provision" ]]; then
      files=$(sudo find "{{ yb_home_dir }}" -maxdepth 1 -mindepth 1  -name "bin" -o -name ".yugabytedb" -o -name "controller" -o -name "master" -o -name "tserver")
    else
      files=$(find "{{ yb_home_dir }}" -maxdepth 1 -mindepth 1  -name "bin" -o -name ".yugabytedb" -o -name "controller" -o -name "master" -o -name "tserver")
    fi
    if [[ -n "$files" ]]; then
        add_result "Yugabyte Home Directory Clean Check" "FAIL" "Directory {{ yb_home_dir }} is not clean"
    else
        add_result "Yugabyte Home Directory Clean Check" "PASS" "Directory {{ yb_home_dir }} is clean"
    fi
  fi

  # Check whether files in data directory are cleaned up.
  data_dir_clean=true
  processes=("tserver" "master" "controller")
  IFS="," read -ra mount_points_arr <<< "$mount_points"
  for path in "${mount_points_arr[@]}"; do
    if [ -e "$path/pg_data" ]; then
      data_dir_clean=false
      break
    fi
    if [ -e "$path/ybc-data" ]; then
      data_dir_clean=false
      break
    fi
    for daemon in "${processes[@]}"; do
      if [ -e "$path/yb-data/$daemon" ]; then
        data_dir_clean=false
        break
      fi
    done
  done
  if [[ "$data_dir_clean" == "true" ]]; then
    add_result "Yugabyte Data Directory Clean Check" "PASS" "Data directories are clean"
  else
    add_result "Yugabyte Data Directory Clean Check" "FAIL" "Data directories are not clean"
  fi
fi
