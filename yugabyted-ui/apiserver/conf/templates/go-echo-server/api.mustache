package handlers
{{#operations}}
import (
    "{{{moduleName}}}{{{outputPath}}}/models"
    "github.com/labstack/echo/v4"
    "net/http"
){{#operation}}

// {{nickname}} - {{{summary}}}
func (c *Container) {{operationId}}(ctx echo.Context) error {
    return ctx.JSON(http.StatusOK, models.HelloWorld {
        Message: "Hello World",
    })
}
{{/operation}}{{/operations}}